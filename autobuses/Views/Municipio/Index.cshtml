@using autobuses.core.Entidades
@model List<Municipio>

<div id="page-wrapper">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Municipios</h1>
            </div>
        </div>

        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-body">

                        <a href="@Url.Action("Registro", "Municipio")" class="btn btn-primary">Agregar nuevo</a>

                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>Nombre</th>
                                        <th>Id</th>
                                        <th>Id del estado</th>
                                        <th>Acciones</th>
                                    </tr>
                                </thead>

                                <tbody>
                                    @foreach (Municipio m in Model)
                                    {
                                        <tr>
                                            <td>
                                                @m.Nombre
                                            </td>
                                            <td>
                                                @m.Id
                                            </td>
                                            <td>
                                                @m.Estado.Id
                                            </td>
                                            <td>
                                                <a href="@Url.Action("Registro", "Municipio", new {id = m.Id})" class="btn btn-primary"><i class="fa fa-edit"></i> Editar</a>
                                                <a class="btn btn-danger"><i class="fa fa-trash-o"></i> Eliminar</a>
                                            </td>
                                        </tr>
                                    }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

